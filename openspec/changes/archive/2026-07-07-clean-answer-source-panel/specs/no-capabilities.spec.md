## No Spec Changes

This change is implementation-only — it modifies the DSPy answer generation pipeline to remove UUID citations from answers. No spec-level requirements are added, modified, or removed. The answer format is an implementation detail, not a spec-level contract.
